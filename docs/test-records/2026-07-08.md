# 测试执行记录 — 2026-07-08

## 测试范围

全量 17 个 Phase 执行（TESTING.md 定义），涵盖 `.editorconfig` marker 三路分支部署、项目结构验证、openspec CLI 集成、Git Worktree、棕地合并、AGENTS.md 门控、Reset、Skill 验证、多语言 i18n、npm pack、注册表 CLI、verify、集成测试。

- **测试执行**: Sisyphus (OpenCode)
- **平台**: Windows 11, PowerShell 7
- **项目版本**: commit `d6dbf06d1` on `main`

---

## 执行结果

| Phase | 名称 | 结果 | 备注 |
|-------|------|------|------|
| **1** | init 测试 | ✅ | 临时目录 init → git commit → dirty 阻止全部通过 |
| **2** | 项目结构验证 | ✅ | 根文件/OPSX 命令(12)/Skill(11)/Schema(9模板)/opencode.json 权限/.gitignore 粒度全部通过 |
| **3** | openspec CLI 集成 | ✅ | Schema 验证/创建变更/依赖链/指令生成/apply blocked/归档全部通过 |
| **4** | Git Worktree 模拟 | ✅ | 单 worktree 创建/隔离/合并 + 双并行 worktree 全部通过 |
| **5** | 棕地 openspec/ 门控 | ✅ | Greenfield/Y(覆盖)/N(退出) 三路全部通过 |
| **6** | 棕地 .opencode/ 合并 | ✅ | commands覆盖/skills跳过/AGENTS.md 三路(追加/替换/跳过)/空文件/单标记全部通过 |
| **7** | Reset 测试 | ✅ | opencode.json+AGENTS.md保留, schemas清理, manifest删除, decision=skip保留 |
| **8** | 棕地 git + 已部署内容 | ✅ | .gitignore/单元测试(39/39)/.editorconfig 棕地四场景全部通过 |
| **9** | Skill/命令定义验证 | ✅ | 11 SKILL.md 全部 >100 chars, 12 个命令文件存在 |
| **10** | 清理 | ✅ | 全部测试目录已删除 |
| **11** | 多语言 --lang | ✅ | 英文/zh-CN/zh-TW help + init zh-CN(初始化完成) + 未知子命令 + ensure-worktree + create-project.js + setup.ps1 DryRun |
| **12** | BROWN_OVERRIDE_* env var | ✅ | 组合测试(openspec=no+commands=yes 门控优先)+绿地非干扰通过 |
| **13** | npm pack | ✅ | tarball 生成成功，含 .gitattributes/AGENTS.md/schema.yaml/skills.lock.json/opencode.json |
| **14** | 注册表 CLI | ✅ | add/list/同名覆盖/verify(不存在+存在)/remove/reset(备份)/非项目目录 |
| **15** | verify 命令 | ✅ | 项目内 5 项检查/exit code 逻辑/非项目目录 ∼ 跳过/损坏注册表检测 |
| **16** | 垫片脚本测试 | ⏸️ | **需用户确认** — 会修改系统 `openspec` 文件 |
| **17** | Registry + Opsx 集成 | ✅ | worktree+registry add → list → verify → remove → reset 完整生命周期 |

### 结果统计

| 指标 | 数值 |
|------|------|
| 总 Phase | 17 |
| 执行并通过 | 15 |
| 跳过(需用户确认) | 1 (Phase 16) |
| 失败 | 0 |

### 已知行为差异/非阻塞问题

- **Phase 6.7 幂等性**：`init` 第二次运行时仍会 prompt openspec/ 门控，未完全按 manifest decisions 自动跳过（需 BROWN_OVERRIDE_OPENSPEC=yes 辅助）
- **Phase 13.4 LF 检查**：Windows 平台无 `xxd` 工具，跳过
- **Phase 14.11**：非项目目录下 `registry list` 透传 `openspec-orig list` 输出 "No active changes found." 而非报错，但功能上可接受
- **Phase 15.5**：损坏注册表上报 "not found" 而非 "parse error"，但 error 行为和 exit code 正确

---

## Phase 详细日志

### Phase 8 — 棕地维度 3/4（git + 已部署内容）验证

#### Phase 8.1 — .gitignore 规则验证

```text
✅ .worktrees/ （regex 匹配）
✅ openspec/schemas/
✅ openspec/config.yaml
✅ openspec/ whole dir NOT gitignored
```

#### Phase 8.2 — 棕地 .gitignore 追加验证

```text
✅ 原始内容 node_modules/ 保留
✅ .worktrees/ 追加
✅ .gitattributes 存在
```

#### Phase 8.3 — Git Deploy 单元测试

测试文件: `test/git-deploy.test.js`

```text
ℹ tests 39
ℹ suites 1
ℹ pass 39
ℹ fail 0
```

#### Phase 8.4 — .editorconfig 棕地部署验证

**8.4.1 绿地部署**
```text
✅ .editorconfig 存在并包含 marker
✅ 包含 root = true
✅ 包含 end_of_line = lf
```

**8.4.2 棕地无标记追加**
```text
✅ 用户内容保留在文件开头
✅ 包含 bridge marker
✅ 用户 indent_style 保留
✅ bridge end_of_line 存在
```

**8.4.3 棕地已有标记替换**
```text
✅ 上方用户内容保留
✅ 下方用户内容保留
✅ 旧 bridge 内容已替换
✅ 模板 end_of_line=lf 生效
```

**8.4.4 棕地已有标记跳过**
```text
✅ 文件内容完全不变（跳过生效）
```
