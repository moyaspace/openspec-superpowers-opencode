# Ensure shell scripts always have LF line endings even on Windows.
# CRLF breaks bash on Linux when installed via npm (the tarball
# preserves whatever line endings are in the working tree).
scripts/setup.sh    text eol=lf
scripts/setup.ps1   text eol=lf
